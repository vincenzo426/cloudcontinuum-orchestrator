---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: pipelineplacementrequests.orchestrator.cloudcontinuum.io
spec:
  group: orchestrator.cloudcontinuum.io
  names:
    kind: PipelinePlacementRequest
    listKind: PipelinePlacementRequestList
    plural: pipelineplacementrequests
    singular: pipelineplacementrequest
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.placementStrategy
      name: Strategy
      type: string
    - jsonPath: .status.targetCluster
      name: Target
      type: string
    - jsonPath: .spec.pipelineName
      name: Pipeline
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PipelinePlacementRequest is the Schema for the pipelineplacementrequests
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PipelinePlacementRequestSpec defines the desired state of
              PipelinePlacementRequest
            properties:
              dataLocation:
                description: DataLocation specifies where the majority of data resides
                enum:
                - edge_cluster_1
                - edge_cluster_2
                - edge_cluster_3
                - cloud_cluster
                - none
                type: string
              dataSize:
                description: |-
                  DataSize specifies the size of data to transfer (e.g., "5GB", "500MB")
                  Used for simulating data transfer time when pipeline is placed away from data location
                type: string
              experimentId:
                description: |-
                  ExperimentId specifies the Kubeflow experiment to use for pipeline runs
                  If empty, a default experiment will be created/used
                type: string
              experimentName:
                default: cloudcontinuum-default
                description: |-
                  ExperimentName specifies the name for the default experiment
                  Only used if ExperimentId is empty
                type: string
              parameters:
                additionalProperties:
                  type: string
                description: Parameters for the pipeline run
                type: object
              pipelineName:
                description: PipelineName is the name for the pipeline
                type: string
              pipelineSource:
                description: PipelineSource specifies where to get the pipeline YAML
                properties:
                  configMapRef:
                    description: ConfigMap reference (if type=configmap)
                    properties:
                      key:
                        description: Key within the ConfigMap
                        type: string
                      name:
                        description: Name of the ConfigMap
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  inline:
                    description: Inline YAML content (if type=inline)
                    type: string
                  type:
                    description: 'Type of source: "url", "configmap", or "inline"'
                    enum:
                    - url
                    - configmap
                    - inline
                    type: string
                  url:
                    description: URL to fetch pipeline YAML (if type=url)
                    type: string
                required:
                - type
                type: object
              placementStrategy:
                description: PlacementStrategy specifies which strategy to use for
                  placement
                enum:
                - cloud-only-pipeline
                - data-locality-pipeline
                - simple-heuristic-pipeline
                - rl-based-pipeline
                - random-pipeline
                type: string
            required:
            - pipelineName
            - pipelineSource
            - placementStrategy
            type: object
          status:
            description: PipelinePlacementRequestStatus defines the observed state
              of PipelinePlacementRequest
            properties:
              conditions:
                description: Conditions represent the current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              dataTransferDetails:
                description: DataTransferDetails provides human-readable information
                  about data transfer calculation
                type: string
              dataTransferLatency:
                description: DataTransferLatency is the network latency in milliseconds
                format: int64
                type: integer
              dataTransferTime:
                description: |-
                  DataTransferTime is the total data transfer time in milliseconds
                  Includes both network latency and actual data transfer based on bandwidth
                format: int64
                type: integer
              experimentId:
                description: ExperimentId used for the run
                type: string
              experimentName:
                description: ExperimentName used for the run
                type: string
              parameters:
                additionalProperties:
                  type: string
                description: Parameters used for the pipeline run
                type: object
              pipelineRunID:
                description: PipelineRunID from Kubeflow
                type: string
              pipelineRunURL:
                description: PipelineRunURL link to Kubeflow UI
                type: string
              placementDecision:
                description: PlacementDecision explains why this cluster was chosen
                type: string
              placementTime:
                description: PlacementTime when the placement decision was made
                format: date-time
                type: string
              targetCluster:
                description: TargetCluster where the pipeline was placed
                type: string
              totalResources:
                description: TotalResources summary of pipeline resource requirements
                properties:
                  executorCount:
                    description: ExecutorCount is the total number of executors in
                      the pipeline
                    type: integer
                  totalCPU:
                    description: TotalCPU is the sum of all CPU requests (in millicores)
                    format: int64
                    type: integer
                  totalGPU:
                    description: TotalGPU is the sum of all GPU requests
                    type: integer
                  totalMemory:
                    description: TotalMemory is the sum of all memory requests (in
                      bytes)
                    format: int64
                    type: integer
                required:
                - executorCount
                - totalCPU
                - totalMemory
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
